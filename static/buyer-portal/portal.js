const $=s=>document.querySelector(s);const enc=new TextEncoder(),dec=new TextDecoder();
async function derive(password,salt){const base=await crypto.subtle.importKey('raw',enc.encode(password),'PBKDF2',false,['deriveKey']);return crypto.subtle.deriveKey({name:'PBKDF2',salt:Uint8Array.from(atob(salt),c=>c.charCodeAt(0)),iterations:250000,hash:'SHA-256'},base,{name:'AES-GCM',length:256},false,['decrypt'])}
async function unlock(password){const payload=await fetch('client.enc.json',{cache:'no-store'}).then(r=>{if(!r.ok)throw Error('configuration');return r.json()});const key=await derive(password,payload.salt);const plain=await crypto.subtle.decrypt({name:'AES-GCM',iv:Uint8Array.from(atob(payload.iv),c=>c.charCodeAt(0))},key,Uint8Array.from(atob(payload.data),c=>c.charCodeAt(0)));return JSON.parse(dec.decode(plain))}
function esc(v=''){return String(v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}
function render(cfg){document.title=`${cfg.firstName}'s Buyer Decision Page | HomeBuyer Experts`;$('#lock').classList.add('hidden');$('#portal').classList.remove('hidden');$('#hello').textContent=`Hello, ${cfg.firstName}.`;$('#intro').textContent=cfg.intro||`Let's capture what stood out while today's conversation is still fresh. Your answers help us understand your thinking, protect what matters to you, and make the next decision with more clarity.`;$('#apptType').textContent=cfg.appointmentType||'Buyer appointment';$('#apptDate').textContent=cfg.appointmentDate||'';$('#focus').textContent=cfg.focus||'Decision debrief';$('#buyerName').value=cfg.household||cfg.firstName;$('#appointment').value=[cfg.appointmentType,cfg.appointmentDate].filter(Boolean).join(' — ');$('#items').innerHTML=(cfg.items||[]).map((x,i)=>`<div class="item"><h3>${esc(x.name)}</h3>${x.note?`<div class="muted">${esc(x.note)}</div>`:''}<div class="pick"><label><input type="radio" name="item_${i}" value="Keep considering">Keep considering</label><label><input type="radio" name="item_${i}" value="Need more information">Need more information</label><label><input type="radio" name="item_${i}" value="Let it go">Let it go</label></div></div>`).join('');$('#customQuestions').innerHTML=(cfg.questions||[]).map((q,i)=>`<div class="question"><label for="q_${i}">${esc(q)}</label><textarea id="q_${i}" name="question_${i+1}" required></textarea></div>`).join('');$('#nextPrompt').textContent=cfg.nextPrompt||'What would make the next step feel clearer or more useful to you?'}
$('#unlockForm').addEventListener('submit',async e=>{e.preventDefault();const b=$('#unlockBtn'),err=$('#unlockError');err.textContent='';b.disabled=true;b.textContent='Opening…';try{render(await unlock($('#password').value))}catch(_){err.textContent='That password did not open this page. Please check it and try again.'}finally{b.disabled=false;b.textContent='Open my decision page'}});
$('#debriefForm').addEventListener('submit',()=>{const btn=$('#submitBtn');btn.disabled=true;btn.textContent='Sending your debrief…'});